import React, { Component } from 'react';
import { AppRegistry, View, Text, StyleSheets, TouchableHighlight, TextInput, Image, ImageBackground, Button, Video, Audio } from 'react-native';
import { Constants } from 'expo';

export default class App extends Component {
  render{
    return(

    );
  }
}


const appStyleSheet = StyleSheets.create({
  container: {
    flex: 1,
    backgroundColor: 'blue'
  }
});
